{{~#each parameters}}{{name}}: {{> models/type schema}},{{/each}}
{{~#each queries}}{{name}}{{#unless required}}?{{/unless}}: {{> models/type schema}},{{/each}}
{{~#if headers}}headers: {
	{{#each headers}}
	{{name}}{{#unless required}}?{{/unless}}: {{> models/type schema}},
	{{/each}}
}{{#if (every headers 'required' false)}} = {}{{/if}},
{{/if}}
{{~#if cookies}}cookies: {
{{~#each cookies}}
	{{name}}{{#unless required}}?{{/unless}}: {{> models/type schema}},
{{/each}}
}{{#if (every cookies 'required' false)}} = {}{{/if}},
{{/if}}
{{~> apis/server servers}} config?: AxiosRequestConfig
